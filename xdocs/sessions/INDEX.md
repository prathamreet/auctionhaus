# Session Index

> Newest on top. One line per session. Click into the file for full notes.

## In Progress

_(none — **Phases A-F all complete; Phase RP (paper reconciliation) landed 2026-06-02.** User actions still outstanding: (1) record `paper/demo.mp4` using the script in `paper/DEPLOY.md`; (2) run `prisma migrate dev` in `back/` to pick up `20260531000000_phase_e_indexes_refunded_at`. Optionally capture a fresh `paper/figures/fraud_perf.json` by hitting `GET /api/fraud/perf` during a k6 run. Optionally also `graphify update .` to refresh the AST graph.)_

## Completed

- **2026-06-03** — [Honest Fraud Pipeline Rebuild (Phase RP-2)](2026-06-02-reaching-paper.md#addendum-6-honest-pipeline-rebuild--paper-rejected-freeze-lifted-2026-06-03) — First paper rejected on **domain scope**, lifting the freeze. Implemented the deferred corrections: new `dataset.ts` (shared replay, real-sellerId σ grounding, deterministic FNV-1a train/test split), multi-auction `run.ts` (primary seller × N auctions + decoy seller so σ genuinely separates fraud), `train.ts` fits on train only + writes classifier directly (v1/v2 fiction removed, `train.v2.ts` deleted), `eval.ts` reports on held-out test with ROC + mean-ablation + two real baselines (outbid-count + best single-feature decision stump). Old single-auction corpus kept under `runs/_paper-snapshot` for provenance (excluded from pipeline). USER re-runs `sim:run ×N → train:fraud → eval:fraud` to regenerate honest numbers, then updates the paper.
- **2026-06-02 (late)** — [Pre-Review Fine-Tune (Phase G)](2026-06-02-reaching-paper.md#addendum-pre-review-fine-tune-same-day-later) — Targeted polish ahead of 4-June review. App-wide `ErrorBoundary` wired in providers; landing-page footer fake metrics removed; wallet zero-balance callout + deposit-cap hint + tx-row description spacing fix; dashboard brand-new-user welcome card; backend log emojis stripped across `index.ts`, `lib/redis.ts`, and every script (rules.md compliance); `seed-users.ts` adds `hoster@x.com` so the create-*-auction scripts work right after `db:seed-users`. Realtime audited — no changes needed (Phase F coverage holds).
- **2026-06-02** — [Reaching the Paper (Phase RP)](2026-06-02-reaching-paper.md) — Post-submission paper reconciliation. `fraud.classifier.ts` restored to paper-snapshot constants with `PAPER_SNAPSHOT: 2026-06-01` banner (threshold 0.20, exact weights from supplementary Tables III + VII). `packages/simulator/src/train.ts` (filename preserved to match supplementary §4.1) carries `--write --confirm` safety guard in place; default invocation writes only to `fraud.classifier.candidate.ts`. New `train.v2.ts` is the corrected (real-sellerId) trainer, writes only to `fraud.classifier.candidate.v2.ts`, never touches the snapshot. `SimRunManifest.auctionOwners` added; `run.ts` records the real seller id in every new manifest. Three paper-era sim runs preserved under `back/packages/simulator/runs/_paper-snapshot/` with README; one post-paper run under `_post-paper/`; empty stray removed. `paper/figures/k6_results.json` + `paper/figures/README.md` pin supplementary Table XII as canonical. `packages/simulator/k6/run-canonical.ps1` is the reproducible six-run k6 sweep runner. `FraudEngine` instrumented with a 1024-sample latency ring and `BidGraph.stats()` returns `approxBytes`; exposed via `GET /api/fraud/perf` and `POST /api/fraud/perf/reset` (admin-only). `reaching-rp.md` strikethroughs + execution log + pull-the-repo defence checklist landed.
- **2026-05-31** — [Phase E + Phase F (Fine-Tune + UI Polish)](2026-05-31-phase-e-fine-tune.md) — **Phase E (ten engineering items + paper + patent):** E1 batched `bid:ladder` socket event; E2 strict `setAutoBid` affordability; E3 deterministic ladder tie-break; E4 ladder `endTime` race guard + anti-snipe ownership comment; E5 two hot-spot indexes; E6 `BidSequencer` XLEN backpressure + `bid:backpressure` event; E7 `Bid.refundedAt` idempotent sealed refunds + ledger-entry gap fixed; E8 `auction:presence` with 250 ms debounce + "N watching" pill; E9 `onReconnect` in socket hooks; E10 `serverTs` on every bid event. P1 `paper/auto-bid-ladder.tex` — 6-page IEEE paper, three correctness theorems, three-way eval. P2 `paper/patent-draft.md` — Indian Form 2 provisional, 1 indep + 8 dep claims. **Phase F (UI/UX polish for live bidding):** F1 `LiveTicker` floating event stack; F2 `ConnectionStatus` Navbar pill + `useConnectionState` hook; F3 `LadderBanner` ephemeral ladder-resolved announcement; F4 BidHistory ladder grouping + auto/manual icons + own-bid tinting; F5 Countdown urgency tri-state + bid input live feedback; F6 `AutoBidHealth` card with capacity bar; F7 `BackpressureBanner` sticky top-banner; F8 7 new CSS keyframes + `prefers-reduced-motion`; F9 plan + session log + `TODAY.md` TLDR.
- **2026-05-30** — [Phase D + Completions](2026-05-30-phase-c.md) — Fixed TypeScript `AuthRequest` in bid.routes; wired `BidSequencer.startConsumer()` in workers; k6 benchmark script (`packages/simulator/k6/bid-throughput.js`); `CommitmentPanel.tsx` frontend for sealed-bid commit/reveal (C6 UI); `packages/simulator/tsconfig.json`; `packages/simulator` added to root workspaces; `docs/hardening.md` Phase A hardening report; `paper/viva-prep.md` three viva questions with 60-second answers + five backup questions; `paper/DEPLOY.md` local + Railway/Vercel deploy steps + 3-min demo script; `paper/tables/metrics.tex` placeholder so paper compiles immediately; `docker-compose.yml` at repo root; Phase D struck in `plan.md`; Definition of Done updated (4/5 checked, 1 user action: record `demo.mp4`).
- **2026-05-30** — [Phase C Research Build](2026-05-30-phase-c.md) — Full Phase C. **C2/C3:** `back/src/modules/fraud/` — `BidGraph` (30-min sliding window, reciprocity), 5-feature extractor, in-process logistic regression classifier (score + explain), `FraudEngine` singleton wired into `bid.service.placeBid` (fire-and-forget after tx commit), emitting `fraud:flag` to `admin:fraud` socket room + persisting `FraudFlag` to Postgres. New `FraudFlag` + `BidCommitment` Prisma models + migration. **C6:** SHA-256 commit-reveal service (`commitment.service.ts`) + 3 new bid endpoints. **C7:** `BidSequencer` Redis Stream consumer + `docs/architecture/sequencer.md` with benchmark table. **C1:** `packages/simulator/` — 4 agent personas (truthful/sniper/shill/collusion) + `run.ts` producing labelled `events.jsonl`. **C5:** `eval.ts` — offline replay, ROC curve, per-feature ablation, LaTeX metrics table. **C4:** `/admin/fraud` fully rewired with live stream, `FeatureBar`, dismiss/suspend actions, top-bidders leaderboard. **C8:** `paper/main.tex` 8-section IEEE paper + `references.bib` (7 cites) + `paper/poster.tex` A1 poster.
- **2026-05-30** — [Phase B Design System](2026-05-30-phase-b-design-system.md) — Full Phase B. 13 ui/* primitives (Button, Input/Textarea/Select, Field, Card, Badge, Skeleton, EmptyState, Stat, Alert, PageHeader/Toolbar/PageShell, Money, Tabs, Countdown, BidChart SVG, ThemeToggle). Dark theme via CSS vars + ThemeBootstrap no-flash script. `useSocketListener` / `useAuctionRoom` hooks (ref-stable, no re-subscribe). `lib/contracts.ts` mirrors back Zod schemas; `useZodForm` hook. `Navbar` converted. All 12 pages fully converted/refactored: 1419-LOC auction detail split into 4 sub-components (BidHistory, KeyDetails/InfoPanel, WinnerCertificate, PricePanel ≈ 250 LOC orchestrator). `/admin/fraud` scaffold page wired to `fraud:flag` socket + heuristic fallback with "detector offline" banner. Responsive `.grid-main-sidebar` + `.auth-grid` CSS helpers added.
- **2026-05-29** — [A10 doc-truth + A11 docs generator](2026-05-29-a10-a11-docs-generator.md) — Closed Phase A. **A10:** confirmed `bible.md`/`done.md`/`back-learn.md` already live under `xdocs/archive/` behind a "do not trust" README; kept `done.md` (harmless + a record of the drift) rather than deleting. **A11:** new `back/src/scripts/generate-docs.ts` + `npm run docs:generate` — pure static analysis (Prisma DSL line-parse + TS compiler-API walk; no app boot, no DB). Reads the `app.use('/api/...')` mount table in `index.ts`, follows each to its `*.routes.ts`, resolves every `router.METHOD` to its controller handler + the Zod schema it runs, derives auth level from middleware, and emits `docs/schema.md` (models/enums) + `docs/api.md` (route tables + request schemas + a "defined but not mounted" section that flags dead `auto-bid.routes.ts`). Deterministic output (no timestamps). **User runs `npm run docs:generate` to emit the files** (no-run rule). Phase A is now fully struck in plan.md.
- **2026-05-29** — [A5 EscrowService + A9.x Invalidation](2026-05-29-a5-escrow-settlement.md) — **A5:** new `back/src/modules/escrow/escrow.service.ts` + Settlement idempotency. **A9.x:** cross-instance suspend invalidation via Redis pub/sub + dedicated `redisInvalidateSub` connection.
- **2026-05-29** — [FTS Search Rewrite](2026-05-29-fts-search-rewrite.md) — `getAuctions` search on GIN index via `to_tsquery` with `:*` prefix operator.
- **2026-05-28** — [Phase A1 Decimal + A3 Indexes + A6 Ladder + A7/A8/A9 + A6.x](2026-05-28-phase-a1-decimal-money.md) — Decimal money, indexes, auto-bid ladder, notification queue, Redis adapter, user cache.
- **2026-05-28** — [Audit & Plan](2026-05-28-audit-and-plan.md) — Deep audit, plan.md as SSOT, Phase A2 row locks, Phase A4 sealed-bid privacy.

## Archive

_(empty — collapse older entries here once INDEX.md grows past ~30 lines)_
