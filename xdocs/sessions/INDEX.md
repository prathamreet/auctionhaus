# Session Index

> Newest on top. One line per session. Click into the file for full notes.

## In Progress

_(none — **Phase A is closed.** A10 + A11 landed this session. Next big thing is Phase B: extract the frontend design system. Phase C is gated on the §7 open questions in plan.md.)_

## Completed

- **2026-05-29** — [A10 doc-truth + A11 docs generator](2026-05-29-a10-a11-docs-generator.md) — Closed Phase A. **A10:** confirmed `bible.md`/`done.md`/`back-learn.md` already live under `xdocs/archive/` behind a "do not trust" README; kept `done.md` (harmless + a record of the drift) rather than deleting. **A11:** new `back/src/scripts/generate-docs.ts` + `npm run docs:generate` — pure static analysis (Prisma DSL line-parse + TS compiler-API walk; no app boot, no DB). Reads the `app.use('/api/...')` mount table in `index.ts`, follows each to its `*.routes.ts`, resolves every `router.METHOD` to its controller handler + the Zod schema it runs, derives auth level from middleware, and emits `docs/schema.md` (models/enums) + `docs/api.md` (route tables + request schemas + a "defined but not mounted" section that flags dead `auto-bid.routes.ts`). Deterministic output (no timestamps). **User runs `npm run docs:generate` to emit the files** (no-run rule). Phase A is now fully struck in plan.md.
- **2026-05-29** — [A5 EscrowService + A9.x Invalidation](2026-05-29-a5-escrow-settlement.md) — **A5:** new `back/src/modules/escrow/escrow.service.ts` exposes `settleWithinTx(tx, {...})` — the single buyer→seller settlement path shared by `buyNow` (DIRECT_SALE: debit balance) and `confirmWinnerPayment` (WON_AUCTION: release heldAmount). Runs inside the caller's locked tx, locks wallets ascending, writes the paired PAYMENT ledger rows, and inserts a new `Settlement` row (model + `SettlementKind` enum + hand-written migration `20260529000000_settlement/`). The `Settlement` row (`auctionId @unique`) is now the idempotency guard, replacing confirmWinnerPayment's old "existing PAYMENT transaction" probe. buyNow + confirmWinnerPayment refactored to call it. 4 new escrow tests; payment/auction tests updated (settlement mock + funded-wallet mock). **A9.x:** cross-instance suspend invalidation — `admin.suspendUser` publishes the userId on a `user:invalidate` Redis channel; `index.ts` subscribes via a dedicated `redisInvalidateSub` connection (not the adapter's `redisSub`) and evicts every instance's auth `userCache`. **Gate:** user must `prisma migrate dev` (regenerates client) before tsc/jest pass — `prisma.settlement` doesn't exist until then (A1 pattern).
- **2026-05-29** — [FTS Search Rewrite](2026-05-29-fts-search-rewrite.md) — Closed the last low-risk Phase A leftover: `getAuctions` search switched from ILIKE `contains` (sequential scan) to Postgres FTS over the existing GIN index. Search is tokenised, each term gets the `:*` prefix operator ("rol" matches "rolex"), ANDed into a bound-parameter `to_tsquery`; only the FTS id-match runs in raw SQL, ids feed back into the existing Prisma `findMany`/`count` via `where.id.in` so filters/pagination/includes/serialization stay type-safe. All-punctuation queries short-circuit to an empty page. 3 tests added. Migration file untouched (Prisma checksum). Deferred with rationale: A5 EscrowService (money-flow refactor + schema, needs user input), cross-instance suspend invalidation (fragile Redis-connection sharing).
- **2026-05-28** — [Phase A1 Decimal + A3 Indexes + A6 Ladder + A7/A8/A9 + A6.x](2026-05-28-phase-a1-decimal-money.md) — **A1:** Every money field switched from Float to `Decimal @db.Decimal(18,2)` in `schema.prisma`. Hand-written migration SQL under `20260528000000_decimal_money/`. New `back/src/lib/decimal.ts` exports `D / toNum / neg / serializeMoney`. All money arithmetic across bid / wallet / auction / payment / auto-bid / workers replaced with Decimal methods. API surface preserved as `number` via `serializeMoney()` at every service-return boundary so the frontend doesn't break. Test helper `__mocks__/money.ts` adds an `m(n)` matcher tolerant of both Decimal and plain number. Stray AUCTION_WON emoji removed. **A3:** B-tree indexes on Auction(status/endTime/type+status), Bid(auctionId+status), Bid(bidderId), Notification(userId+isRead), Transaction(userId+createdAt) + GIN tsvector(title||description) added in `20260528000001_perf_indexes/migration.sql` with matching `@@index([...])` in `schema.prisma`. FTS-search query rewrite in `getAuctions` deferred (index in place, one-query follow-up). **A6:** New `autoBidQueue` in `queues/auction.queue.ts` + new `autoBidWorker` + exported `processAutoBidLadder()` in `workers/index.ts`. Ladder runs in ONE `prisma.$transaction`, one-increment per step (the UX contract — every 110, 120, 130 ... row shows in the bid log), bounded by `(highestMax − cur)/inc + 2`. Producer is `bid.service.placeBid` enqueueing AFTER its tx commits. Old recursive `processAutoBids` deleted (nested tx + violated per-step contract). 4 new ladder tests in `workers/index.test.ts`. Deferred: ladder trigger on `setAutoBid` upsert; anti-snipe extension on ladder rungs.
- **2026-05-28** — [Audit & Plan](2026-05-28-audit-and-plan.md) — Deep audit of monorepo, identified doc/code drift, wrote `plan.md` as SSOT, archived AI-generated docs into `xdocs/archive/`, set up this sessions/ folder. Then **Phase A4 fully closed**: sealed-bid privacy fixed across REST (`bid.service`, `auction.service`), socket emit (`bid.controller`), and source (`placeBid` no longer updates `currentPrice` for sealed). Frontend `Bid` type widened. Tests added in both service test files. Then **Phase A2 fully closed**: `FOR UPDATE` pessimistic locks landed in placeBid / withdraw / buyNow / endAuction / confirmWinnerPayment under a global lock-order (auction first, then wallets ASC). Withdraw and buyNow check-then-act races eliminated. endAuction idempotent under BullMQ re-delivery. Global jest mock defaults added so existing tests pass through transparently. Bonus: `setAutoBid` now rejects sealed auctions (was a Phase A6 follow-up identified earlier). Tiny housekeeping fix in `back/README.md`.

## Archive

_(empty — collapse older entries here once INDEX.md grows past ~30 lines)_
