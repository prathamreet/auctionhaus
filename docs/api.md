<!-- GENERATED by `npm run docs:generate` (back/src/scripts/generate-docs.ts).
     Do not edit by hand -- re-run the generator after changing the code. -->

# HTTP API

Derived from the `app.use('/api/...', ...)` mount table in `back/src/index.ts`, the mounted `*.routes.ts` files, and the Zod schemas each handler runs. **Auth** column: `Public` = no token; `Auth` = JWT required; `Admin` = JWT + admin role.

## Root

| Method | Path |
|---|---|
| GET | `/health` |

## `/api/auth`  — src/modules/auth/auth.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| POST | `/api/auth/register` | Public (rate-limited) | `authController.register` | body |
| POST | `/api/auth/login` | Public (rate-limited) | `authController.login` | body |
| GET | `/api/auth/me` | Auth | `authController.getMe` | — |

#### Request schemas

**POST `/api/auth/register`** (body)
- `name`: `z.string().min(2).max(50)`
- `email`: `z.string().email()`
- `password`: `z.string().min(6)`

**POST `/api/auth/login`** (body)
- `email`: `z.string().email()`
- `password`: `z.string().min(1)`

## `/api/users`  — src/modules/users/user.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/users/me` | Auth | `userController.getProfile` | — |
| PUT | `/api/users/me` | Auth | `userController.updateProfile` | body |
| GET | `/api/users/me/bids` | Auth | `userController.getBidHistory` | — |
| GET | `/api/users/me/auctions` | Auth | `userController.getMyAuctions` | — |
| GET | `/api/users/me/won` | Auth | `userController.getWonAuctions` | — |
| POST | `/api/users/rate` | Auth | `userController.rateUser` | body |
| GET | `/api/users/:id` | Auth | `userController.getProfile` | — |

#### Request schemas

**PUT `/api/users/me`** (body)
- `name`: `z.string().min(2).max(50).optional()`
- `avatar`: `z.string().url().optional()`

**POST `/api/users/rate`** (body)
- `rateeId`: `z.string().uuid()`
- `auctionId`: `z.string().uuid()`
- `score`: `z.number().int().min(1).max(5)`
- `comment`: `z.string().max(500).optional()`

## `/api/auctions`  — src/modules/auctions/auction.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/auctions` | Public | `auctionController.getAuctions` | query |
| GET | `/api/auctions/:id` | Public | `auctionController.getAuctionById` | — |
| POST | `/api/auctions` | Auth | `auctionController.createAuction` | body |
| PUT | `/api/auctions/:id` | Auth | `auctionController.updateAuction` | body |
| DELETE | `/api/auctions/:id` | Auth | `auctionController.cancelAuction` | — |
| POST | `/api/auctions/:id/buy-now` | Auth | `auctionController.buyNow` | — |

#### Request schemas

**GET `/api/auctions`** (query)
- `status`: `z.nativeEnum(AuctionStatus).optional()`
- `type`: `z.nativeEnum(AuctionType).optional()`
- `search`: `z.string().optional()`
- `page`: `z.coerce.number().int().positive().default(1)`
- `limit`: `z.coerce.number().int().positive().max(100).default(20)`

**POST `/api/auctions`** (body)
- `title`: `z.string().min(3).max(200)`
- `description`: `z.string().min(10)`
- `imageUrl`: `z.string().url().optional()`
- `type`: `z.nativeEnum(AuctionType).default('ENGLISH')`
- `startingPrice`: `z.number().positive()`
- `reservePrice`: `z.number().positive().optional()`
- `buyNowPrice`: `z.number().positive().optional()`
- `dutchPriceStep`: `z.number().positive().optional()`
- `dutchInterval`: `z.number().int().positive().optional()`
- `minIncrement`: `z.number().positive().default(1)`
- `antiSnipingMins`: `z.number().int().min(0).max(30).default(5)`
- `startTime`: `z.string().datetime().optional()`
- `endTime`: `z.string().datetime().optional()`

**PUT `/api/auctions/:id`** (body)
- `title`: `z.string().min(3).max(200).optional()`
- `description`: `z.string().min(10).optional()`
- `imageUrl`: `z.string().url().optional()`

## `/api/bids`  — src/modules/bidding/bid.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| POST | `/api/bids/auctions/:auctionId` | Auth | `bidController.placeBid` | body |
| POST | `/api/bids/auctions/:auctionId/stream` | Auth | `bidController.placeBidStream` | body |
| GET | `/api/bids/auctions/:auctionId` | Auth | `bidController.getAuctionBids` | — |
| GET | `/api/bids/auctions/:auctionId/auto-bid` | Auth | `autoBidController.getMyAutoBid` | — |
| POST | `/api/bids/auctions/:auctionId/auto-bid` | Auth | `autoBidController.setAutoBid` | body |
| DELETE | `/api/bids/auctions/:auctionId/auto-bid` | Auth | `autoBidController.cancelAutoBid` | — |
| POST | `/api/bids/auctions/:auctionId/commit` | Auth | `bidController.commitBid` | body |
| POST | `/api/bids/auctions/:auctionId/reveal` | Auth | `bidController.revealBid` | body |
| GET | `/api/bids/auctions/:auctionId/commitments` | Auth | `bidController.getCommitments` | — |

#### Request schemas

**POST `/api/bids/auctions/:auctionId`** (body)
- `amount`: `z.number().positive()`

**POST `/api/bids/auctions/:auctionId/stream`** (body)
- `amount`: `z.number().positive()`

**POST `/api/bids/auctions/:auctionId/auto-bid`** (body)
- `maxAmount`: `z.number().positive()`

**POST `/api/bids/auctions/:auctionId/commit`** (body)
- `commitHash`: `z.string().length(64)`

**POST `/api/bids/auctions/:auctionId/reveal`** (body)
- `amount`: `z.number().positive()`
- `nonce`: `z.string().min(8)`

## `/api/wallet`  — src/modules/wallet/wallet.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/wallet` | Auth | `walletController.getWallet` | — |
| POST | `/api/wallet/deposit` | Auth | `walletController.deposit` | body |
| POST | `/api/wallet/withdraw` | Auth | `walletController.withdraw` | body |
| GET | `/api/wallet/transactions` | Auth | `walletController.getTransactions` | query |

#### Request schemas

**POST `/api/wallet/deposit`** (body)
- `amount`: `z.number().positive()`

**POST `/api/wallet/withdraw`** (body)
- `amount`: `z.number().positive()`

**GET `/api/wallet/transactions`** (query)
- `page`: `z.coerce.number().int().positive().default(1)`
- `limit`: `z.coerce.number().int().positive().max(100).default(20)`

## `/api/notifications`  — src/modules/notifications/notification.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/notifications` | Auth | `notificationController.getNotifications` | query |
| PUT | `/api/notifications/read-all` | Auth | `notificationController.markAllRead` | — |
| DELETE | `/api/notifications/all` | Auth | `notificationController.deleteAllNotifications` | — |
| PUT | `/api/notifications/:id/read` | Auth | `notificationController.markRead` | — |
| DELETE | `/api/notifications/:id` | Auth | `notificationController.deleteNotification` | — |

#### Request schemas

**GET `/api/notifications`** (query)
- `page`: `z.coerce.number().int().positive().default(1)`
- `limit`: `z.coerce.number().int().positive().max(100).default(20)`

## `/api/watchlist`  — src/modules/watchlist/watchlist.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/watchlist` | Auth | `watchlistController.getWatchlist` | — |
| POST | `/api/watchlist` | Auth | `watchlistController.addToWatchlist` | body |
| DELETE | `/api/watchlist/:auctionId` | Auth | `watchlistController.removeFromWatchlist` | — |

#### Request schemas

**POST `/api/watchlist`** (body)
- `auctionId`: `z.string().uuid()`

## `/api/admin`  — src/modules/admin/admin.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/admin/dashboard` | Admin | `adminController.getDashboard` | — |
| GET | `/api/admin/users` | Admin | `adminController.getAllUsers` | query |
| PUT | `/api/admin/users/:userId/suspend` | Admin | `adminController.suspendUser` | body |
| GET | `/api/admin/auctions` | Admin | `adminController.getAllAuctions` | query |
| PUT | `/api/admin/auctions/:auctionId/moderate` | Admin | `adminController.moderateAuction` | body |
| GET | `/api/admin/fraud-flags` | Admin | `adminController.getFraudFlags` | — |

#### Request schemas

**GET `/api/admin/users`** (query)
- `page`: `z.coerce.number().int().positive().default(1)`
- `limit`: `z.coerce.number().int().positive().max(100).default(20)`
- `search`: `z.string().optional()`

**PUT `/api/admin/users/:userId/suspend`** (body)
- `suspend`: `z.boolean()`

**GET `/api/admin/auctions`** (query)
- `page`: `z.coerce.number().int().positive().default(1)`
- `limit`: `z.coerce.number().int().positive().max(100).default(20)`
- `status`: `z.nativeEnum(AuctionStatus).optional()`

**PUT `/api/admin/auctions/:auctionId/moderate`** (body)
- `action`: `z.enum(['cancel', 'activate'])`

## `/api/payments`  — src/modules/payments/payment.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| POST | `/api/payments/auctions/:auctionId/confirm` | Auth | `paymentController.confirmPayment` | — |

## `/api/fraud`  — src/modules/fraud/fraud.routes.ts

| Method | Path | Auth | Handler | Request |
|---|---|---|---|---|
| GET | `/api/fraud/flags` | Admin | `fraudController.getFraudFlags` | query |
| GET | `/api/fraud/stats` | Admin | `fraudController.getFraudStats` | — |
| PUT | `/api/fraud/flags/:id/dismiss` | Admin | `fraudController.dismissFlag` | — |

#### Request schemas

**GET `/api/fraud/flags`** (query)
- `dismissed`: `z.coerce.boolean().optional().default(false)`
- `limit`: `z.coerce.number().int().positive().max(200).optional().default(50)`
- `auctionId`: `z.string().uuid().optional()`
- `bidderId`: `z.string().uuid().optional()`
